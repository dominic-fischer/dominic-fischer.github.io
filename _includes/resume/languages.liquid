<div class="list-groups">
  {% for content in data[1] %}
    <div class="list-group col-md-6">
      <table class="table-cv list-group-table">
        <tbody>
          <tr>
            <!-- Calculate colspan number for category title -->
            {% assign i = 1 %}
            {% for item in content.items %}
              {% assign i = i | plus: 1 %}
            {% endfor %}
            <td colspan="{{ i }}" class="list-group-category">
              {% if content.emoji_path %}
                <img src="{{ content.emoji_path }}" alt="emoji" style="width: 1em; height: 1em; vertical-align: middle; margin-right: 0.3em;">
                {{ content.language }}
              {% else %}
                {{ content.language | emojify }}
              {% endif %}
            </td>
          </tr>
          <tr>
            <td class="list-group-name"
              title="{{ content.fluency | emojify }}"
              style="cursor: help;">
              {% assign is_latin = content.classical == true %}
              {% assign max_level = 6 %}
              {% assign level = content.fluency_level | plus: 0 %}
              {% for i in (1..max_level) %}
                {% if i <= level %}
                  <span style="display:inline-block;width:16px;height:16px;background:{% if is_latin %}#6f6f6f{% else %}#2a9d8f{% endif %};border:1px solid #ddd;margin-right:2px;border-radius:2px;"></span>
                {% else %}
                  <span style="display:inline-block;width:16px;height:16px;background:#eee;border:1px solid #ddd;margin-right:2px;border-radius:2px;"></span>
                {% endif %}
              {% endfor %}
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  {% endfor %}
</div>
